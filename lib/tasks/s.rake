#namespace :s do
#  desc "Sobe o servidor rapidamente"
#  task s: :environment do
#      %x(rails s -b 0.0.0.0)
# end
#       spinner = TTY::Spinner.new("[:spinner] Chupacabra Entrando online...", format: :shark)
#      spinner.auto_spin # Automatic animation with default interval
      
  
#    sleep(5) # Perform task

#    spinner.stop("ON") # Stop animation

#end